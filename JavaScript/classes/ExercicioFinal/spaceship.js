export default class Spaceship {
    constructor(name, numberOfCrew, type){
        this.name = name
        this.numberOfCrew = numberOfCrew
        this.type = type
        this.velocity = 0
    }
}